import { ComponentStyleConfig } from '@chakra-ui/react';

export const Select: ComponentStyleConfig = {
  baseStyle: {
    field: {
      color: 'primary',
      border: '1px solid',
      borderColor: 'primary',
      borderRadius: '2xl',
      bgColor: 'transparent',
      _hover: {
        borderColor: 'primary',
      },
    },
  },
};
